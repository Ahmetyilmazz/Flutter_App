import 'package:flutter/material.dart';

class Aramalar extends StatelessWidget {
  const Aramalar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("DENEME -2"),
      ),
    );
  }
}
