import 'package:flutter/material.dart';

class Sohbetler extends StatelessWidget {
  const Sohbetler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("DENEME -1"),
      ),
    );
  }
}
